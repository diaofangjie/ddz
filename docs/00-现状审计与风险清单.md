# 00 - 现状审计与风险清单

> 审计时间：2026-09-15　审计范围：`D:\dfj\dfj\ddz`（本地源码）+ `8.137.195.155`（阿里云线上）
> 证据类型标注：【实测】= 本次实际探测/读取得到；【推断】= 由代码结构推导。

---

## 1. 项目基本盘

### 1.1 线上环境【实测】

| 探测项 | 结果 | 说明 |
|--------|------|------|
| `8.137.195.155:22` | ✅ 开放 | SSH 可登录 |
| `8.137.195.155:80` | ✅ 开放，HTTP 200 | 返回前端页面（`<title>` 为斗地主，`theme-color: #2d5a27`）→ Nginx 托管 `client/build` |
| `8.137.195.155:443` | ❌ 关闭 | **无 HTTPS** |
| `8.137.195.155:3001` | ✅ 开放，`GET /health` → `200 {"status":"ok"}` | Node 后端存活（应为 pm2 守护） |
| `8.137.195.155:8080` | ❌ 关闭 | 本地开发端口，未对公网暴露（正确） |

**结论：服务器可用，当前服务正在正常运行。** 后端进程健康，前端已在 80 端口对外服务。

> ⚠️ 待补：CPU / 内存 / 磁盘 / Node 版本 / pm2 进程列表 / Nginx 配置 需要 SSH 登录后确认，见第 4 节。

### 1.2 技术栈【实测】

| 层 | 技术 | 版本 |
|----|------|------|
| 后端运行时 | Node.js | 服务器按文档安装的是 18.x |
| 后端框架 | Express | ^4.18.2 |
| 实时通信 | Socket.IO | ^4.7.2 |
| 进程守护 | pm2 | 文档推荐，线上应为 `ddz-server` |
| 前端框架 | React | ^18.2.0 |
| 前端构建 | react-scripts (CRA) | 5.0.1 |
| 样式 | styled-components | ^6.1.1 |
| 实时客户端 | socket.io-client | ^4.7.2 |
| 移动端打包 | Capacitor | 已生成 android/ 与 ios/ 工程 |
| 数据持久化 | **无** | 全部状态在进程内存 |

### 1.3 代码规模【实测】

| 文件 | 大小 | 行数 | 评价 |
|------|------|------|------|
| `server/src/GameLogic.js` | 27.6 KB | 747 | 偏大，承载规则+状态+计分+AI |
| `server/src/index.js` | 21.2 KB | 543 | 偏大，混了房间生命周期/AI/超时/序列化/路由 |
| `server/src/CardLogic.js` | 12.4 KB | 349 | 相对健康（接近纯函数） |
| `server/src/RoomManager.js` | 8.4 KB | 242 | 可接受 |
| **后端合计** | | **1881** | |
| `client/src/components/GameScreen.js` | **81.8 KB** | ~2000+ | 🔴 巨型组件，全站前端约 60% 集中在这一个文件 |
| `client/src/components/RoomScreen.js` | 15.5 KB | | |
| `client/src/components/HomeScreen.js` | 13.6 KB | | |
| `client/src/App.js` | 10.9 KB | 344 | socket 事件总线集中在此 |
| `client/src/components/Card.js` | 4.1 KB | | |

### 1.4 已实现的玩法能力【实测 + 推断】

3 人斗地主、4 人斗地主、创建/加入房间、房间号邀请、癞子玩法、封顶倍数、局数选择（4/8/16）、文字聊天、断线重连、离线 AI 托管、加倍阶段、弃牌阶段、队伍模式（`setTeamMode` / `assignPlayerTeam`）、炸弹规则、记牌器、PWA、APK/iOS 打包。

> 功能面确实"已具备较多自有功能"，这也决定了**不应重写，而应外科手术式重构**。

---

## 2. 风险清单

严重度：🔴 P0 阻断/安全 · 🟠 P1 严重 · 🟡 P2 改善

### 🔴 P0-1 项目没有版本控制【实测】

`D:\dfj\dfj\ddz\.git` **不存在**，但存在 `.gitignore`。目录里还混着 `client/build/`（构建产物）、`ddz-alibaba-deploy.zip`、`repomix-output.xml`。

**风险**：任何重构都不可回滚，一次误删即灾难。这是所有工作的**前置阻断项**。
**处理**：`git init` → 补全 `.gitignore` → 首次全量提交 → 打 tag `v1.0.0-baseline`（详见 06 文档阶段 0）。

### 🔴 P0-2 手牌全量广播（可作弊）【实测】

`server/src/index.js:119-143` 的 `serializeRoom()` **原样保留了 `room.game` 全量字段**（含 `game.hands` = 所有玩家手牌），随后在 `index.js:146 / 431 / 493 / 507 / 521 / 539 / 556` 等处通过 `io.to(room.id).emit(..., serializeRoom(room))` 发给了**房间内所有人**。

**风险**：任何玩家打开浏览器 DevTools → Network → WS，即可实时看到对手手牌与底牌。对"好友娱乐"是体验问题，对"要优化成可持续项目"是**架构级缺陷**。
**处理**：引入**视图裁剪** `projectFor(room, viewerId)`，见 `01-目标架构设计.md` 第 3 节。

### 🔴 P0-3 客户端自报身份（可伪造）【实测】

所有动作事件都直接采信 payload 里的 `playerId`：`index.js:491`(doubling)、`505`(discard)、`519`(callLandlord)、`534`(playCards)、`554`(pass)。服务端**不校验 socket ↔ player 的绑定关系**，也不校验"是否轮到你"。

**风险**：改一个字段就能替别人出牌；配合 P0-2 可完全操控牌局。
**处理**：身份改由服务端签发并与 socket 强绑定，见 P0-4 与 ADR-0003。

### 🔴 P0-4 无会话/令牌机制【实测 + 推断】

`断线重连说明.md` 明确要求"必须使用**完全相同的玩家名**"才能重连，`playerId` 由客户端生成并存在 localStorage。

**风险**：名字即身份 → 抢注他人昵称即可顶替；玩家 ID 可任意伪造。
**处理**：首次连接由服务端签发 `sessionToken`（随机不可猜），客户端持久化，重连时凭 token 恢复身份。

### 🔴 P0-5 无 HTTPS（443 未开放）【实测】

前端 `http://` 分享、Socket.IO 走明文 `ws://`。
**风险**：微信/部分移动浏览器对 `http` 站点限制（麦克风、通知、剪贴板等 API 不可用，分享卡片降级）；流量可被监听篡改；无证书无法上 CDN/域名。
**处理**：申请域名 → Nginx 终结 TLS → 反代 `/socket.io` 到 3001，见 `04-部署与运维手册.md`。

### 🔴 P0-6 状态无持久化【实测】

房间与对局全部在进程内存 Map 中（`RoomManager`），无快照、无落库。
**风险**：`pm2 restart`、进程 OOM、服务器重启 → 所有进行中的对局瞬间消失；无法做战绩、无法复盘。
**处理**：定期快照 + 对局结束后落库（SQLite 起步），见 `01-目标架构设计.md` 第 7 节。

---

### 🟠 P1-1 广播路径不一致（真实 bug）【实测】

代码里有两条并行路径：

- 路径 A（**未序列化**）：`index.js:75 / 80 / 85 / 91 / 103 / 259 / 264 / 269 / 275 / 290 / 302 / 352 / 359 / 365 / 382 / 389` 直接 `emit('gameUpdated', currentRoom)` 传**原始对象**。
- 路径 B（**已序列化**）：其余位置传 `serializeRoom(room)`。

原始对象里 `readyPlayers` 是 `Set`，`JSON.stringify` 后变成 `{}`；`matchHistory` 里的数组也可能带非标准结构。

**风险**：客户端在超时/AI 触发路径下收到 `readyPlayers: {}`，`Array.isArray` 判断失败 → 准备状态显示错乱、下一局无法开始等偶发 bug。这类"偶发"最难查。
**处理**：统一为**单一出口** `broadcast(room)`，见 `01-目标架构设计.md` 第 4 节。

### 🟠 P1-2 客户端发出的事件服务端没有实现【实测】

`client/src/App.js:290` 发送 `voteExtend`（延长局数投票），但服务端 `index.js` 的 `io.on('connection')` 中**没有 `voteExtend` 的 handler**（已全量检索确认）。

**风险**：用户点了按钮界面无反应、无报错 —— 静默失效的功能。
**处理**：要么补齐实现，要么移除入口。记入路线图阶段 4 需求池。

### 🟠 P1-3 超时 / 离线托管 / AI 三段逻辑重复【实测】

同一套"按阶段决定默认动作"的逻辑被写了三遍：

- `startTurnTimer()` — `index.js:50-116`
- `handleOfflinePlayerAction()` — `index.js:241-314`
- `handleAIAction()` — `index.js:322-399`

每段都硬编码了 `calling / doubling / discarding / playing` 四个分支。

**风险**：新增一个阶段（或改 4 人局规则）要改三处并保证三处一致，是**后续迭代最大的事故源**。
**处理**：抽成单一 `defaultActionFor(phase, seat)` + 统一调度器 `advance(room)`，见 `01-目标架构设计.md` 第 5 节。

### 🟠 P1-4 递归 + setTimeout 取代调度器【实测】

`handleAIAction` 内部递归调用自身（`index.js:353/360/366/383/390`），每个动作后 `setTimeout(1000)` 再触发。代码中已出现"重新检查 room / currentPlayer 是否变化"的补丁注释（`index.js:252-254 / 342-344`），说明竞态已经真实发生过。

**风险**：长局下调用链变深、多房间并发时行为不可预测。
**处理**：改为"动作完成 → 触发一次 `advance()`"的单向调度。

### 🟠 P1-5 单文件过大 / 职责混杂【实测】

`GameScreen.js` 81.8 KB；`index.js` 543 行；`GameLogic.js` 747 行同时承担规则判定、状态转移、计分、AI 决策。

**风险**：改动波及面不可控；编辑器/模型上下文装不下，每次修改都在"盲改"。
**处理**：按 `01-目标架构设计.md` 的模块切分，**渐进式**拆分（不搞一次性大搬家）。

### 🟠 P1-6 无自动化测试【实测】

仅有根目录两个手写脚本：`test-bomb-rules.js`、`test-no-shuffle.js`；`server/package.json` 无 `test` 脚本；无 CI。

**风险**：重构无安全网 —— 这正是 P1-3/P1-5 想动却不敢动的原因。
**处理**：先给 `CardLogic`（纯函数）补单测，建立"先有网，再拆房"的顺序，见 `05-测试与质量保障.md`。

### 🟠 P1-7 源码目录混入产物与存档【实测】

`client/build/`（构建产物）、`client/build.zip`、`ddz-alibaba-deploy.zip`、`repomix-output.xml`、`.idea/`、`.vercel/` 与源码同级。

**风险**：仓库膨胀、误提交、构建产物与源码不同步导致"改了没生效"。
**处理**：纳入 `.gitignore`，产物改为发布时生成。

### 🟠 P1-8 硬编码 IP 与僵尸配置【实测】

- `client/src/config.js:10` 硬编码 `http://8.137.195.155:3001`。
- `client/.env` 与已构建的 `main.*.js` 中残留 `REACT_APP_SERVER_URL=http://localhost:3001`，但 **`config.js` 并未读取该变量**（读的是 `process.env.NODE_ENV`）→ 僵尸配置，误导后人。

**风险**：换服务器/上域名需改代码重新构建，且极易漏改；僵尸配置会让人误以为改 `.env` 生效。
**处理**：改为**同域相对路径**（`/socket.io` 由 Nginx 反代），彻底移除 IP 和 `.env` 依赖。

### 🟡 P2-1 CORS 全开【实测】

`server/src/index.js:23-26` `origin: '*'`。改为同域反代后应收紧到具体域名。

### 🟡 P2-2 房间列表全量广播【实测】

`broadcastRoomList()` 在每次房间变动时向**所有连接**广播（`index.js:316-319`）。当前规模无碍，但玩家数增长后是无效流量。
**处理**：仅向"在大厅的客户端"广播，或改为按需拉取。

### 🟡 P2-3 依赖版本陈旧且 CRA 已停止维护【推断】

`react-scripts@5.0.1` 官方已不再积极维护，构建慢、依赖树告警多。
**处理**：低优先级，可择机迁 Vite（见 ADR-0001 的"暂不做"部分）。

### 🟡 P2-4 安全组开放 22 给 `0.0.0.0/0`【实测，来自 `ALIYUN_DEPLOY.md`】

文档中建议 `22` 端口"实在不会可先填 `0.0.0.0/0`"。
**处理**：收紧为固定来源 IP；禁用密码登录，改密钥；装 fail2ban。

---

## 3. 风险修复优先级排序

```mermaid
graph LR
    A["阶段0 止血<br/>P0-1 版本控制"] --> B["阶段0 安全<br/>P0-3/4 身份绑定"]
    B --> C["阶段0 安全<br/>P0-2 视图裁剪"]
    C --> D["阶段1 可测试<br/>P1-6 单测网"]
    D --> E["阶段2 重构<br/>P1-1/3/4/5 架构"]
    E --> F["阶段3 在线<br/>P0-5/6 + 10人房"]
    F --> G["阶段4 体验<br/>P1-2 需求池"]
```

顺序不可颠倒的原因：**没有版本控制就不敢改；没有身份绑定，视图裁剪也守不住；没有测试网，重构就是赌运气。**

---

## 4. 待补充的审计项（需要 SSH 登录后执行）

以下信息本次无法通过公网探测获得，需要服务器 Shell 访问：

```bash
# 系统与规格
cat /etc/os-release
nproc && free -h && df -h
uptime

# 运行时
node -v && npm -v
pm2 list
pm2 describe ddz-server

# 反向代理
nginx -v
cat /etc/nginx/sites-available/default    # 或 conf.d/*.conf

# 安全组/防火墙实际状态
ufw status
ss -tulnp | grep -E '80|443|3001'

# 域名解析（若有）
dig +short <你的域名>
```

**把输出贴回来，我可以据此把 `04-部署与运维手册.md` 里的"待确认"全部落定。**

---

## 5. 审计结论

| 维度 | 评价 |
|------|------|
| 功能完整度 | 🟢 高。3/4 人玩法、癞子、加倍、弃牌、队伍、托管、重连均有实现 |
| 线上可用性 | 🟢 良好。服务存活，前后端均可达 |
| 代码健康度 | 🟠 中下。核心文件过大、逻辑三处重复、无分层 |
| 安全性 | 🔴 差。可作弊（手牌泄露 + 身份伪造）、明文传输、无 HTTPS |
| 工程规范 | 🔴 差。无版本控制、无测试、无 CI、产物混入源码 |
| 可维护性 | 🟠 差。文档与代码脱节（文档描述的是"理想状态"，代码是"实际状态"） |

**总体判断：这是一个"功能跑得动、但地基没打好"的项目。**
正确策略不是重写（重写的代价远高于收益，且现有玩法细节会大量丢失），而是：
**先用版本控制兜底 → 再补安全与测试 → 然后按模块渐进重构 → 最后叠加在线规模能力。**
